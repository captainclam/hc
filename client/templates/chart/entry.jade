li.entry(style="background-image:url({{image}})")
  .entry__remove x
  img.entry__thumb(src="{{image}}")
  .entry__details
    .title.entry__title {{title}}
    .subtitle.entry__subtitle {{subtitle}}
